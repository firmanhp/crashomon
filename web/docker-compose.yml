services:
  crashomon-web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    env_file:
      - path: .env
        required: false
    ports:
      - "5000:5000"
    volumes:
      - crashomon-data:/data
    restart: unless-stopped

volumes:
  crashomon-data:
